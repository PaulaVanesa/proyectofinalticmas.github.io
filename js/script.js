
var typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    typeSpeed: 40, //Velocidad de tipeo de cada palabra
    backSpeed:20, //Velocidad de borrado
    loop:true,
    backDelay: 2500, //Tiempo de espera en la pabra antes que se borre
    smartBackspace: true,
  });